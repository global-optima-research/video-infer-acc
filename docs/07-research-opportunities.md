# 研究机会分析

> 调研日期：2026-03-06

基于对视频模型推理加速 6 大方向的全面调研，识别出以下研究空白和机会。

---

## 机会总览

| 优先级 | 方向 | 新颖性 | 工程量 | 潜在影响 |
|--------|------|--------|--------|----------|
| **1** | 少步模型缓存失效 + 协同设计 | 高 | 中 | 直接出论文 |
| **2** | DuCa 随机性理论推广 | 很高 | 低 | 理论贡献大 |
| **3** | 长视频专项加速 | 高 | 中 | 实际需求强 |
| **4** | 自适应组合框架 | 中 | 高 | 工程价值大 |
| **5** | 统一 benchmark | 中 | 高 | 社区价值大 |
| **6** | 消费级 GPU 端到端优化 | 低 | 中 | 用户群大 |

---

## 1. 少步模型的缓存失效问题 ★ 最推荐

### 现状

- 步数蒸馏（DCM/DOLLAR）将 50 步压缩到 4 步，每步信息密度大幅增加
- TeaCache/AdaCache 等缓存方法为 50 步设计（相邻步差异小，>97% 相似）
- 4 步模型中每步都很关键，步间相似性假设可能不成立

### 研究问题

1. 蒸馏后的 4 步模型上，各缓存方法是否还有效？衰减多少？
2. 4 步模型中 block 级 / token 级缓存是否比 step 级缓存更合适？
3. DCM+TeaCache 报告 54x 加速，但这个组合是否最优？
4. 能否在蒸馏过程中引入缓存感知（cache-aware distillation）？

### 可能的贡献

- 首个系统研究蒸馏 × 缓存交互效应的工作
- 设计专门针对少步模型的缓存策略
- cache-aware distillation：蒸馏时优化 student 的步间特征相似性，使其对缓存更友好

### 实验方案

```
基线模型：Wan2.1-14B (50步)
蒸馏模型：DCM 4步 / DOLLAR 4步

实验 1：在 4 步模型上测试所有缓存方法
  - TeaCache / AdaCache / DuCa / MixCache / EOC
  - 对比 50 步模型上的加速比和质量衰减

实验 2：粒度对比
  - Step 级缓存 vs Block 级缓存 vs Token 级缓存
  - 在 4 步 vs 50 步模型上的效果差异

实验 3：协同设计
  - 蒸馏 loss 中加入步间特征相似性正则项
  - 缓存策略参与蒸馏过程的联合训练
```

---

## 2. DuCa "随机 ≈ 重要性" 的理论推广

### 现状

DuCa 发现：在 token 缓存中，**随机选择 token ≈ 按注意力分数选择 token**，且随机选择天然保证多样性，还兼容 FlashAttention。

这是一个深刻但未被充分探索的 insight。

### 研究问题

1. 这个现象是否普遍？在以下场景中是否同样成立：
   - 随机选择哪些 block 缓存 vs 按相似度选择？
   - 随机选择哪些 layer 量化 vs 按敏感度分析选择？
   - 随机选择蒸馏的中间步骤 vs 精心设计的 schedule？
2. 如果"随机"普遍有效，说明什么？
   - Diffusion 模型内部是否存在**均匀分布的信息冗余**？
   - 这是否与 diffusion 模型的去噪过程（逐步去除均匀噪声）有关？
3. 随机选择的理论最优比例是多少？与模型规模/步数/分辨率的关系？

### 可能的贡献

- 建立"随机选择在扩散模型中普遍近似最优"的理论解释
- 信息论视角：量化 diffusion 模型中冗余的分布均匀性
- 实践指导：如果随机选择足够好，则无需复杂的选择策略，大幅简化方法设计

### 实验方案

```
实验 1：跨粒度验证
  - Block 选择：随机 vs 相似度 vs 梯度
  - Token 选择：随机 vs attention vs L2-norm
  - Step 选择：随机跳过 vs TeaCache 自适应
  在 3+ 模型上验证

实验 2：信息论分析
  - 计算各 block/token/step 的互信息
  - 验证是否均匀分布

实验 3：最优随机比例
  - 扫描 10%-90% 随机比例
  - 在不同模型规模和步数下的 Pareto 前沿
```

---

## 3. 长视频加速的 Scaling 规律

### 现状（经验证，非完全空白）

**已有相关工作**：
- **LongLive** (NVIDIA, ICLR'26) — 实时长视频生成（30s-240s），short window attention + frame sink，20.7 FPS
- **HiStream** (2025.12) — 3 轴冗余消除（空间/时间/步数），anchor-guided sliding window，76-107x 加速
- **Video-Infinity** (2024.06) — 分布式 clip 并行，8 GPU 生成 2300 帧
- **BlockVid** (2025.11) — 半自回归块扩散 + 语义感知 KV cache，分钟级视频
- **SANA-Video** (NVIDIA, ICLR'26 Oral) — Block Linear DiT + constant-memory KV cache，分钟级 720p
- **FlowCache** (2026.02) — 首个自回归视频缓存框架（MAGI-1, SkyReels-V2）
- **Quant VideoGen** (2026.02) — 2-bit KV cache 量化，自回归长视频 ~700 帧
- **AdaSpa** (ICCV'25) — 自适应稀疏注意力，**唯一画了加速比 vs 视频长度曲线**（最长 24s，1.59x→4.01x）

**确认的空白**：
- TurboDiffusion (200x)、TeaCache、STA 等主流加速方法**全部在 2-5 秒短视频上验证**
- **没有论文系统研究加速方法随视频长度增长的性能变化规律**（30s/60s/120s 上各方法表现如何？）
- **没有论文对比缓存误差随长度的累积模式**
- BLADE 论文自己承认这个 gap："extending to minute-long videos remains an important next step"
- AdaSpa 是最接近的——画了 scaling 曲线，但只到 24 秒就停了

### 精确定位

~~首个系统研究长视频加速的工作~~ → **首个系统研究推理加速技术随视频长度扩展的性能变化规律的工作**

核心问题：在 5s/15s/30s/60s/120s 上对主流加速方法（缓存/稀疏注意力/蒸馏/量化）做 scaling 曲线，揭示长度依赖的冗余模式和误差累积规律。

### 研究问题

1. 主流加速方法（TeaCache/STA/蒸馏/FP8）在 30s/60s/120s 上的加速比和质量衰减曲线？
2. 缓存误差是否随长度线性累积？还是存在相变点（如场景切换时突然恶化）？
3. 长视频的冗余分布与短视频有何不同？场景内 vs 场景间的冗余模式？
4. 能否设计长度感知的自适应策略？（短视频用一套参数，长视频自动调整）

### 可能的贡献

- 首个加速方法 × 视频长度的系统 scaling 研究（5s-120s）
- 长视频特有的误差累积规律和相变点发现
- 长度感知的自适应加速策略

### 需要 acknowledge 的先行工作

- HiStream 的 3 轴冗余框架（聚焦高分辨率而非长时间）
- AdaSpa 的长度 scaling 曲线（只到 24s）
- LongCat 的 Block Sparse Attention（没做加速方法横向对比）
- FlowCache / Quant VideoGen（聚焦自回归范式，非 DiT 加速方法对比）

---

## 4. 自适应组合框架

### 现状

- 6 个加速方向正交可叠加（理论 108x，实测 50-200x）
- 但所有组合都是**手动堆叠、静态配置**
- TurboDiffusion（200x）是当前最好的组合，但它是固定配置
- AdaCache 的"运动感知"和 MixCache 的"三粒度自适应"证明了自适应思路可行

### 研究问题

1. 给定输入 prompt + 硬件约束 + 质量要求，如何自动选择最优方法组合？
2. 能否用轻量预测模型（prompt → 最优配置）替代人工调参？
3. 各方法之间是否存在负交互（叠加后 1+1<2）？如何检测和规避？

### 可能的贡献

- 自适应加速配置选择器
- 方法交互效应的系统研究
- 类似 AutoML 的自动加速配置搜索

### 设想

```
输入: prompt + target_quality + hardware + latency_budget
  │
  ▼
[轻量分析器] → prompt 复杂度 / 运动强度估计
  │
  ▼
[配置选择器] → 从预定义配置空间中选择最优组合
  │
  ├── 简单场景 → 激进缓存 (TeaCache aggressive) + 少步 + 高量化
  ├── 复杂场景 → 保守缓存 + 更多步数 + 低量化
  └── 低端 GPU → 自动启用 offloading + 更激进量化 + VAE tiling
  │
  ▼
输出: 最优配置 + 预估质量/速度
```

---

## 5. 统一 benchmark

### 现状

- 每篇论文用不同的模型、分辨率、帧数、评测指标
- 无法公平对比不同方向的方法
- FastGen 有蒸馏 benchmark，FastVideo 有推理 benchmark，但没有跨方向的统一基准

### 研究问题

1. 如何设计涵盖所有 6 个方向的统一评测框架？
2. 质量指标如何兼顾感知质量和时间一致性？
3. 如何评测方法的叠加效果？

### 可能的贡献

```
VideoAccelBench:
├── 模型固定: Wan2.1-14B + HunyuanVideo
├── 分辨率/帧数: 480p/720p, 81/121 帧
├── 评测维度:
│   ├── 速度: 端到端延迟 + DiT-only 延迟 + 首帧延迟
│   ├── 质量: VBench + FVD + 人工偏好
│   ├── 资源: 峰值显存 + 平均 GPU 利用率
│   └── 鲁棒性: 100 条不同复杂度 prompt 的方差
├── 方法覆盖: 6 个方向 × 各方向 top-3 方法
└── 组合测试: 推荐组合的叠加效果
```

---

## 6. 消费级 GPU 端到端优化

### 现状

- RTX 4090 是性价比最优选择（$0.005/视频 vs 云 API $0.15-$2.50）
- 但大部分论文在 H100/A100 上做实验
- 24GB VRAM 是硬约束，需要不同的优化策略

### 研究问题

1. 4090 上各方法的最优组合是什么？（与 H100 可能不同）
2. VRAM 受限下，offloading vs 量化 vs tiling 的最优权衡？
3. 面向 5090 (32GB) 和未来 RTX 6090 的前瞻优化？

### 可能的贡献

- 消费级 GPU 专项加速指南
- 一键部署工具（类似 llama.cpp 对 LLM 的意义）
- VRAM 感知的自动配置

---

## 交叉机会

```
机会 1 (缓存×蒸馏) ←──互补──→ 机会 2 (随机性理论)
       │                              │
       │  理论 2 可解释为什么          │
       │  缓存在少步模型中             │
       │  失效或仍然有效               │
       │                              │
       ▼                              ▼
机会 4 (自适应框架) ←──需要──→ 机会 5 (统一 benchmark)
       │                              │
       │  框架需要 benchmark            │
       │  来评估配置效果               │
       │                              │
       ▼                              ▼
机会 3 (长视频) ←──应用场景──→ 机会 6 (消费级 GPU)
```

**最高效路径**：从机会 1（缓存×蒸馏）或机会 2（随机性理论）入手，工程量小，学术新颖性高，且能为后续机会 4/5 提供基础。

---

## 参考

- 本调研全部文档：[README](../README.md)
- 技术全景图：[panorama.md](panorama.md) | [在线可视化](https://video-infer-acc.optima.sh/)
- 各方向深入调研：[01](01-step-distillation.md) | [02](02-feature-caching.md) | [03](03-token-pruning-sparse-attention.md) | [04](04-quantization.md) | [05](05-vae-pipeline-optimization.md) | [06](06-hardware-deployment.md)
